# **Data Analysis Training – Daily Diary (Day 14)**

**Date:** ___________

Today was the fourteenth day of my Data Analysis training. The session focused on advanced concepts of **NumPy** and **Pandas**, where I practiced performing numerical computations and analyzing real-world datasets using Python.

The session began with **NumPy array operations**. I practiced creating two-dimensional arrays using random values and learned how to modify array elements based on specific conditions. I also explored array manipulation techniques such as **flattening** multi-dimensional arrays into one-dimensional arrays and **reshaping** arrays into different dimensions according to the requirements of a program.

Next, I learned how to generate evenly spaced values using the **`linspace()`** function and applied mathematical functions such as **`sin()`** to perform numerical computations on arrays. I also practiced calculating row-wise averages using aggregation functions like **`mean()`** and filtering data based on computed values.

The trainer then demonstrated various **element-wise arithmetic operations** on NumPy arrays, including addition, subtraction, multiplication, and division. These exercises helped me understand the efficiency of vectorized operations in NumPy without using explicit loops.

The second half of the session focused on **Pandas**. I learned how to read CSV files into a DataFrame using the **`read_csv()`** function and explored datasets using functions such as **`head()`** and **`describe()`** to obtain statistical summaries and understand the structure of the data.

I also practiced creating new columns by calculating the **total** and **average** values of rows. The trainer explained how to perform basic data analysis by comparing values with statistical measures such as the mean and filtering records that satisfy specific conditions.

Finally, I learned about **grouping data** using the **`groupby()`** function and handling **missing values**. I practiced identifying null values with **`isna()`** and replacing missing data using the **`fillna()`** method. These techniques are important for cleaning and preparing datasets before performing data analysis.

Overall, today's session strengthened my understanding of advanced NumPy operations and practical data analysis using Pandas. The hands-on exercises improved my ability to manipulate arrays, analyze datasets, perform statistical calculations, and clean missing data, which are essential skills for real-world data analysis and machine learning projects.
